for plotting heatmaps: source ~/.bashrc && conda activate model_metamers_pytorch && python model_analysis_folders/audio_networks/spectemp_filters/mms_plotting/create_metamer_heatmaps.py --pickle_dir model_analysis_folders/audio_networks/spectemp_filters/mms_plotting/merged_metamers --output_dir model_analysis_folders/audio_networks/spectemp_filters/mms_plotting/heatmaps


Yay, amazing thanks so much. Now, can we  do the following:

Sometimes, we will have multiple runs for the same soundID, randomseed, and subclip. If that happens, I'd like to select the ones with the highest R^2 and the lowest MSE and 1) plot the better one on the heatmap, and 2) make a merged_metamers_betteroptimized folder with the files that are best for each tuple (if it's only one then it brings that one over) does that make sense to you as a selection process?
perhaps we can make two heatmaps, one that includes all runs and one that includes the best optimized ones?
